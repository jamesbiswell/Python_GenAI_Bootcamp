import sys

# List all available modules (uncomment to use)
# help('modules')

# Exit the program (commented out to prevent unintended exits)
# sys.exit()

# sys.argv stores command-line arguments (useful for scripts)
# print(sys.argv)

# Print the list of directories where Python looks for modules
print(sys.path)

# Print the current Python version
print(f'Current Python version: {sys.version}')
